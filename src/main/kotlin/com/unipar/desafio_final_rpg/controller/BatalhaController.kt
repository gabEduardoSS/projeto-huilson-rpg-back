package com.unipar.desafio_final_rpg.controller

/*import com.unipar.desafio_final_rpg.model.Turno
import com.unipar.desafio_final_rpg.service.BatalhaService
import org.springframework.web.bind.annotation.PostMapping
import org.springframework.web.bind.annotation.RequestBody
import org.springframework.web.bind.annotation.RequestMapping
import org.springframework.web.bind.annotation.RestController

@RestController
@RequestMapping("/batalha")
class BatalhaController(

    private val batalhaService: BatalhaService
) {

     // Executa uma rodada da batalha
    @PostMapping("/turno")
    fun executarTurno(
        @RequestBody request: Turno
    ): String {

        return batalhaService.executarTurno(request)
    }
}*/